<Window x:Class="ArendatorPraktPm11.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Муниципальная Аренда" Height="600" Width="550"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Window.Resources>
        <LinearGradientBrush x:Key="WindowBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#1a237e" Offset="0"/>
            <GradientStop Color="#0d47a1" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="55"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45a049"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="BorderBrush" Value="#ccc"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="12,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="BorderBrush" Value="#ccc"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="White" CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="12,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="FontSize" Value="15"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBackground}">
        <Border Background="White" CornerRadius="25" Margin="25" Padding="30">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Заголовок -->
                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,35">
                    <TextBlock Text="🏢 Муниципальная Аренда" FontSize="26" FontWeight="Bold" Foreground="#1a237e"/>
                    <TextBlock Text="г. Слободской, Кировская область" FontSize="14" Foreground="Gray" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>

                <!-- Форма входа -->
                <StackPanel Grid.Row="1" VerticalAlignment="Center">

                    <TextBlock Text="Выберите роль" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="cmbRole" SelectedIndex="0" Style="{StaticResource ModernComboBox}">
                        <ComboBoxItem Content="👤 Арендатор" Tag="tenant" FontSize="15" Height="35"/>
                        <ComboBoxItem Content="👔 Сотрудник администрации" Tag="employee" FontSize="15" Height="35"/>
                    </ComboBox>

                    <TextBlock Text="Логин или Email" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="txtLogin" Style="{StaticResource ModernTextBox}"/>

                    <TextBlock Text="Пароль" Style="{StaticResource LabelStyle}"/>
                    <PasswordBox x:Name="txtPassword" Style="{StaticResource ModernPasswordBox}"/>

                </StackPanel>

                <!-- Сообщение об ошибке (теперь ПЕРЕД кнопкой) -->
                <TextBlock x:Name="lblError" Grid.Row="2" 
                           Foreground="#d32f2f" Visibility="Collapsed" 
                           TextWrapping="Wrap" Margin="0,10,0,15" 
                           FontSize="13" FontWeight="SemiBold"
                           HorizontalAlignment="Center"/>

                <!-- Кнопка входа -->
                <Button Grid.Row="3" Content="Войти в систему" Click="btnLogin_Click" 
                        Style="{StaticResource LoginButtonStyle}"/>
            </Grid>
        </Border>
    </Grid>
</Window>